import { CompStyleDirective } from './comp-style.directive';

describe('CompStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CompStyleDirective();
    expect(directive).toBeTruthy();
  });
});
